node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Cached price data — datasets.lock.json is committed; raw CSVs live on 0G Storage.
data/*.csv

# Paper engine per-token snapshots (generated by `bacend paper backfill`).
# Reproducible from chain state + Binance archive; rotates per redeploy.
data/paper/
